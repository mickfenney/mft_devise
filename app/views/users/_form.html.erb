<%= simple_form_for(@user) do |f| %>

<%= f.fields_for :locations do |builder| %>
  <%= render 'users/location_fields', f: builder %>
<% end %>
<%= link_to_add_fields "Add Location", f, "users/", :locations %>

<% end %>