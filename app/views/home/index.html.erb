<div class="content round">
  <div class="row">
    <div class="span8">

      <%= render 'layouts/messages' %>
      <h3>Home</h3>
      <% @users.each do |user| %>
        <p>User: <%=link_to user.name, user %></p>
      <% end %>

    </div>
  </div>
</div>