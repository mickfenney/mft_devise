<div id="featureCarousel" class="carousel slide">
  <!-- Carousel items -->
  <div class="carousel-inner">
    <div class="item active">
    <%= image_tag('1.jpg') %>
      <div class="carousel-caption clearfix">
        <p>
          <% unless user_signed_in? %>
            <div class="pull-right"><%= link_to image_tag('logo_white_small.png'), new_user_session_path %></div>
          <% else %>
            <div class="pull-right"><%= image_tag('logo_white_small.png') %></div>
          <% end %>
        </p>
      </div>
    </div>
    <div class="item">
      <%= image_tag('2.jpg') %>
      <div class="carousel-caption clearfix">
        <p>
          <% unless user_signed_in? %>
            <div class="pull-right"><%= link_to image_tag('logo_white_small.png'), new_user_session_path %></div>
          <% else %>
            <div class="pull-right"><%= image_tag('logo_white_small.png') %></div>
          <% end %>
        </p>
      </div>
    </div>
    <div class="item">
      <%= image_tag('3.jpg') %>
      <div class="carousel-caption clearfix">
        <p>
          <% unless user_signed_in? %>
            <div class="pull-right"><%= link_to image_tag('logo_white_small.png'), new_user_session_path %></div>
          <% else %>
            <div class="pull-right"><%= image_tag('logo_white_small.png') %></div>
          <% end %>
        </p>
      </div>
    </div>
    <div class="item">
      <%= image_tag('4.jpg') %>
      <div class="carousel-caption clearfix">
        <p>
          <% unless user_signed_in? %>
            <div class="pull-right"><%= link_to image_tag('logo_white_small.png'), new_user_session_path %></div>
          <% else %>
            <div class="pull-right"><%= image_tag('logo_white_small.png') %></div>
          <% end %>
        </p>
      </div>
    </div>    
  </div>
  <!-- Carousel nav -->
  <a class="carousel-control left" href="#featureCarousel" data-slide="prev">&lsaquo;</a>
  <a class="carousel-control right" href="#featureCarousel" data-slide="next">&rsaquo;</a>
</div>