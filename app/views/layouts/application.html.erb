<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "mfTechnology" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "mfTechnology" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %> round">
   <header class="navbar navbar-fixed-top">
     <div class="navbar-inner">
       <div class="container">
         <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
         </a>
         <%= link_to "mfTechnology", root_path, :class => 'brand' %>
         <div class="nav-collapse collapse" id="main-menu">
            <%= render 'layouts/navigation' %>
         </div>
       </div>
     </div>
   </header>
   <container>
    <div id="main" role="main">
      <div class="container">
        <%= yield %>
      </div> <!--! end of .container -->
      <br />
      <%= render 'layouts/footer' %>

      <%= debug(params) if Rails.env.development? %>
    </container>
  </body>
</html>
